# Runtime checkpoint state — don't commit (contains in-progress task IDs)
meshy-state.json

# Web-UI fallback list is regenerated each run — don't commit
meshy-failed-fallback.md

# Optimized props output dir — temporary work area, replace originals when done
../assets/models/props-optimized/
